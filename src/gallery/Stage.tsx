// src/gallery/Stage.tsx (temporary stub — replaced in Task 8)
export function Stage() {
  return (
    <main style={{
      background: 'var(--arrange-bg)',
      height: '100%',
      color: 'var(--text)',
      padding: '16px',
      fontFamily: 'var(--font-ui)',
    }}>
      Stage
    </main>
  )
}
